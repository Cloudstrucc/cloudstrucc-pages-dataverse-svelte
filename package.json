{
  "name": "cloudstrucc-pages-dataverse",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "validate": "python3 scripts/validate_repo.py",
    "pack:fallback": "python3 scripts/pack_fallback.py",
    "test": "npm run validate",
    "build": "npm run validate && npm run pack:fallback"
  },
  "devDependencies": {}
}